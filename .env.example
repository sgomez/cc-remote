# Auto-generated by ./setup.sh (config.js -> config.json -> .env). Don't hand-edit
# this expecting it to survive a rerun — edit config.json or answer the prompts.
#
# Infra only. There are no host paths here: every agent container mounts named
# Docker volumes exclusively (a workspace volume per Session, a config volume per
# Account). Accounts are registered in the web UI, and each Session names itself
# and picks its repo there — none of that lives in this file.

# Git identity used for commits Claude makes inside agent containers.
# Auto-detected from the host's global git config; no prompt.
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your.email@example.com"

# Permission mode is NOT configured here. It is a per-Session choice made on the
# create-session form, and its deployment-wide default is a Deployment Setting
# edited in the browser (Settings page) and stored in the database — the single
# source, so there is no environment value that could contradict it.

# --- Web Manager & Caddy ---
# Domain name (or host:port) where the manager is exposed.
DOMAIN_NAME="cc.example.com"
# Public origin better-auth signs cookies / builds the OAuth callback against
# (https://<domain> behind Caddy, or http://localhost:4000 locally).
BETTER_AUTH_URL="https://cc.example.com"
# Stable secret better-auth uses to sign sessions. MUST stay constant across
# restarts (a new one logs everyone out) and be >= 32 chars. setup.sh generates one.
BETTER_AUTH_SECRET="change-me-to-a-64-char-random-hex-string-generated-by-setup"

# Internal port for the web manager (default 4000).
WEB_PORT="4000"
# Caddy HTTP port mapping (e.g. 80:80, or 127.0.0.1:40080:80 to disable public HTTP).
CADDY_HTTP_PORT_BIND="80:80"
# Caddy HTTPS port mapping (default 443).
CADDY_HTTPS_PORT="443"
# Active compose profiles. "caddy" to run Caddy, empty to run without it.
COMPOSE_PROFILES="caddy"

# --- Agent Resource Limits (per container, not a fleet total) ---
# Applied to EVERY container web-manager creates: Sessions, clone helpers, Login
# Containers and the short-lived volume helpers. Without them one runaway build in
# an agent can exhaust the host's RAM and take down every Session *and* web-manager.
#
# setup.sh DERIVES these from the host's real RAM/core count (web-manager can't: the
# socket proxy blocks `docker info`), so you normally never touch them. To change
# them, edit the "resources" block in config.json and rerun ./setup.sh — this file is
# recompiled from config.json, so a hand-edit here does not survive.
#
# Memory: human units (512m, 2g, 1.5g) or raw bytes. Minimum 6m (Docker's own);
# under ~512m Claude Code is unusable. `0` disables the limit (not recommended).
# Docker's swap defaults to 2x the limit, so MemorySwap is pinned equal to Memory —
# swap off, hard ceiling. On a host with swap accounting disabled Docker prints a
# warning and the memory cap still applies.
# An unparseable value makes web-manager refuse to start rather than run unbounded.
AGENT_MEMORY_LIMIT="2g"
# CPU quota in cores (1, 1.5, 2). Keeps a runaway build from starving web-manager.
AGENT_CPU_LIMIT="2"
# Fork-bomb ceiling.
AGENT_PIDS_LIMIT="4096"

# --- Host File Permission Mapping (User Identity Adapter) ---
# Container user/group IDs, matched to the host user so files written into the
# workspace volume aren't root-owned. Auto-detected from `id -u` / `id -g`.
PUID="1000"
PGID="1000"
