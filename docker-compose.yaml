
services:
  claude-agent:
    image: cc-remote-claude-agent
    build: .
    profiles:
      - agent
    restart: unless-stopped
    tty: true
    stdin_open: true
    environment:
      - GITHUB_REPO=${GITHUB_REPO}
      - GIT_USER_NAME=${GIT_USER_NAME}
      - GIT_USER_EMAIL=${GIT_USER_EMAIL}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - HOME=/home/node
      - SESSION_NAME=${SESSION_NAME}
      - SESSION_UUID=${SESSION_UUID}
      - PERMISSION_MODE=${PERMISSION_MODE:-auto}
    volumes:
      - ${PROJECT_PATH}:/workspace
      # NOTE: CLAUDE_CONFIG_PATH and CLAUDE_JSON_PATH are shared read-write between the
      # host and all agent sessions, so a compromised session can alter the host's Claude
      # config. This is an accepted tradeoff for sharing auth across sessions.
      - ${CLAUDE_CONFIG_PATH}:/home/node/.claude
      - ${CLAUDE_JSON_PATH}:/home/node/.claude.json

  docker-socket-proxy:
    # TODO: pin to a specific digest for reproducibility once vetted (tag pinned to v0.4.2 for now)
    image: tecnativa/docker-socket-proxy:v0.4.2
    restart: unless-stopped
    environment:
      - CONTAINERS=1
      - VOLUMES=1
      - POST=1
      - IMAGES=0
      - NETWORKS=0
      - EXEC=0
      - BUILD=0
      - INFO=0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # No ports are published to the host; only reachable from other services on the
    # compose network (e.g. web-manager via DOCKER_HOST=tcp://docker-socket-proxy:2375).

  web-manager:
    build: ./web
    restart: unless-stopped
    ports:
      - "127.0.0.1:${WEB_PORT:-4000}:4000"
    depends_on:
      - docker-socket-proxy
    environment:
      - WEB_PORT=4000
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - ALLOWED_GITHUB_USERS=${ALLOWED_GITHUB_USERS}
      # NOTE: CLAUDE_CONFIG_PATH and CLAUDE_JSON_PATH are shared read-write between the
      # host and all agent sessions, so a compromised session can alter the host's Claude
      # config. This is an accepted tradeoff for sharing auth across sessions.
      - CLAUDE_CONFIG_PATH=${CLAUDE_CONFIG_PATH}
      - CLAUDE_JSON_PATH=${CLAUDE_JSON_PATH}
      - GIT_USER_NAME=${GIT_USER_NAME}
      - GIT_USER_EMAIL=${GIT_USER_EMAIL}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - PERMISSION_MODE=${PERMISSION_MODE:-auto}
      - AGENT_IMAGE=cc-remote-claude-agent
      - DOCKER_HOST=tcp://docker-socket-proxy:2375
      - JWT_SECRET=${JWT_SECRET:-}
      - BASE_URL=${BASE_URL:-}
      - AGENT_PIDS_LIMIT=${AGENT_PIDS_LIMIT:-4096}

  caddy:
    image: caddy:2
    restart: unless-stopped
    profiles:
      - caddy
    ports:
      - "${CADDY_HTTP_PORT_BIND:-80:80}"
      - "${CADDY_HTTPS_PORT:-443}:443"
    environment:
      - DOMAIN_NAME=${DOMAIN_NAME}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web-manager

volumes:
  caddy_data:
  caddy_config:





