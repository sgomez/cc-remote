node_modules/
.output/
.nitro/
.tanstack/
*.tsbuildinfo

# SQLite database (holds plaintext credentials + OAuth tokens) and its WAL
# sidecars — never commit. `data/` is the default DATABASE_PATH dir (#17).
data/
*.db
*.db-wal
*.db-shm

# MikroORM writes a schema snapshot named after the active dbName; it varies by
# environment (temp DB in tests, data/ in prod), so keep it out of the tree.
src/adapters/db/migrations/.snapshot*.json

# Generated by the TanStack Start vite plugin on dev/build.
src/routeTree.gen.ts
